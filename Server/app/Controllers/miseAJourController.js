const DbConnected = require("../../DB/db_config");

/*
var sql = "INSERT INTO Siminaire (Id_Doctorant,titre,resume) VALUES ?";
var values = [
  [54647, "loremIpsum", "heyy1"],
  [54648, "loremIpsum", "heyy2"],
];

DbConnected.query(sql, [values]);
console.log("record inserted successfully");
*/

var sql = "SELECT * FROM "
const reinscription = 15;
const changement_theese = 30;
